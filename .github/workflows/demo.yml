name: demo

on:
  workflow_dispatch:

jobs:
  boundary-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Demo scaffold
        run: |
          echo "cnp-boundary demo scaffold"
          echo "external authority integration is not part of this public workflow"