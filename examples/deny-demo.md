# Deny demo

This example represents a change containing a forbidden pattern such as a secret-like token.

Expected outcome:
- authority returns deny
- client verifies signature
- workflow fails before merge